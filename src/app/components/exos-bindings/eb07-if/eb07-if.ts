import { Component } from '@angular/core';
import { FormsModule } from '@angular/forms';

@Component({
  selector: 'app-eb07-if',
  imports: [FormsModule],
  templateUrl: './eb07-if.html',
  styleUrl: './eb07-if.scss',
})
export class Eb07If {
  temperature = 20;
}
